import styled from "styled-components";

const About = () => {
  return (
    <Wrapper>
      About Me
    </Wrapper>
  );
};

export default About;

const Wrapper = styled.section`
  height: 100vh;
  position: relative;
`;